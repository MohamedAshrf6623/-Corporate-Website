@model Department

<div class="card" style="width: 18rem;">
    <div class="card-body">
        <h5 class="card-title">@Model.Name</h5>
        <p class="card-text">@Model.Manager</p>
        <a href="#" class="btn btn-primary">@Model.Students.Count</a>
    </div>
</div>